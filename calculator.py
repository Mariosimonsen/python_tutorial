# List of calculator functions need to be added

from operator import add, sub, mul, truediv, mod, pow

operators = {
    '+': add,
    '-': sub,
    '*': mul,
    '/': truediv,
    '%': mod,
    '**': pow
}